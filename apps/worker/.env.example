# Backend Core API URL
BACKEND_URL=http://localhost:8000

# Worker Service Configuration
HOST=0.0.0.0
PORT=8001

# Deterministic Policy Defaults
MAX_EXTRA_FARE=5000.0
MAX_STOPS=1
CABIN_CLASS=Economy
MIN_LAYOVER_MINUTES=60
MAX_LAYOVER_MINUTES=360

# Optional LLM Provider Key (Resilient fallback to deterministic explainer if empty)
OPENAI_API_KEY=
